class Wall {
    constructor(public height: number, public width: number, public x: number, public y: number) {}
}

class Prism {
    constructor(public height: number, public width: number, public depth: number, public x: number, public y: number, public z: number) {}
}

export { Wall, Prism };